<h1> New User </h1>

<%= form_for(@user) do |f| %>

  <% if @user.errors.any? %>
    <% @user.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
  <% end %>

  <%= f.label :username %>
  <%= f.text_field :username %>

  <%= f.submit "Create my username" %>

<% end %>